import styled from 'styled-components';

export const StylesAccordion = styled.section`
    font-family: Verdana;
    width: 800px;

    h1 {
        text-transform: capitalize;
        margin-bottom: 30px;
        text-align: center;
    }
`;
